def generate_hashtag(s):
    if len(s)==0:
        return False
    temp=s.split()
    res='#'
    for i in temp:
        res+=i.capitalize()
    if len(res)==0 or len(res)>140:
        return False
    return res

def unique_in_order(sequence):
    res=[]
    if len(sequence)==0:
        return []
    if len(sequence)==1:
        return list((sequence))
    for i in sequence:
        if not res:
            res.append(i)
        elif i!=res[-1]:
            res.append(i)
    return res

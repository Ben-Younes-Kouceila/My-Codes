def spin_words(sentence):
    list=sentence.split()
    res=[]
    for i in list:
        if len(i)>=5:
            res.append(i[::-1])
        else:
            res.append(i)
    return " ".join(res)
